## Project No: 0020A
## Topic: Django Forms

**Objective**
This project focuses on understanding:

- Django Forms (forms.Form)
- Form Validation
- Handling GET & POST Requests
- Collecting User Input

---
# What are Django Forms?
Forms are used to:
- Collect user input
- Validate data
- Process data

---
# Why Use Django Forms?
- Automatic validation
- Built-in security (XSS, SQL Injection)
- Less code
- Easy error handling

---
# Types of Forms
## 1️⃣ HTML Form (Basic)
```html
<form method="post">
    <input type="text" name="username">
</form>
```

❌ No validation  
❌ Not recommended  

---
## 2️⃣ forms.Form (Without Database)
✔ Used for:
- Contact form
- Feedback form
- Login form

---
## 3️⃣ forms.ModelForm (With Database)
✔ Used for:
- CRUD operations  
✔ Connected to models  

---
## 4️⃣ Authentication Forms
- UserCreationForm
- AuthenticationForm
- PasswordResetForm

---
# Memory Trick
```
Form → No DB  
ModelForm → With DB
```

---
# Step 1 — Create Project
```bash
django-admin startproject contactproject
cd contactproject
```

---
# Step 2 — Create App
```bash
python manage.py startapp contact
```

---
# Step 3 — Register App
📄 contactproject/settings.py

```python
INSTALLED_APPS = [
    'contact',
]
```

---
# Step 4 — Create Form
📄 contact/forms.py

```python
from django import forms

class ContactForm(forms.Form):

    name = forms.CharField(max_length=50)
    email = forms.EmailField()
    message = forms.CharField(widget=forms.Textarea)
```

---
# Step 5 — Create View
📄 contact/views.py
```python
from django.shortcuts import render
from .forms import ContactForm

def contact_view(request):

    if request.method == "POST":
        form = ContactForm(request.POST)

        if form.is_valid():
            name = form.cleaned_data['name']
            email = form.cleaned_data['email']
            message = form.cleaned_data['message']

            print(name, email, message)

            return render(request, 'success.html')

    else:
        form = ContactForm()

    return render(request, 'contact.html', {'form': form})
```

---
# Step 6 — Create URLs
📄 contact/urls.py
```python
from django.urls import path
from .views import contact_view

urlpatterns = [
    path('', contact_view, name='contact'),
]
```

---
📄 contactproject/urls.py
```python
from django.contrib import admin
from django.urls import path, include

urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('contact.urls')),
]
```

---
# Step 7 — Create Templates
📁 contact/templates/
---
## contact.html

```html
<h2>Contact Form</h2>

<form method="post">
    {% csrf_token %}
    {{ form.as_p }}
    <button type="submit">Submit</button>
</form>
```

---
## success.html
```html
<h2>Thank You!</h2>
<p>Your message submitted successfully.</p>
```

---
# Step 8 — Run Migrations
```bash
python manage.py migrate
```

---
# Step 9 — Run Server
```bash
python manage.py runserver
```

---
# Output
```
http://127.0.0.1:8000/
```

---
# What Happens Internally
```
User → Form → View → Validation → Response
```

---
# Important Concepts
## POST Data
```python
request.POST
```

---
## Validation
```python
form.is_valid()
```

---
## Cleaned Data
```python
form.cleaned_data
```

---
## Security
```html
{% csrf_token %}
```

---
# Flow
```
Browser → URL → View → Form → Validation → Template
```

---
# Concepts Covered
- forms.Form
- GET vs POST
- Form Validation
- CSRF Protection
- User Input Handling