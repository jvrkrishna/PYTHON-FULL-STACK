This function is used to get max value from a column.
Syntax:
    select max(column_name) from <table_name>;
Ex:
    select max(sal) from EMP_INFO;

Practical:
3️⃣ MAX() – Highest Value
SELECT MAX(SALARY) AS Highest_Salary FROM EMP_INFO;
🔸 Returns: 9500

