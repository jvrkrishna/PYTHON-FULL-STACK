This functions is used to get the sum of numeric column.
Syntax:
    select sum(column_name) from <tablename>;
Ex:
    select sum(Sal) from EMP_INFO;

Practical:
CREATE TABLE EMP_INFO (
    ID INT,
    NAME VARCHAR(50),
    DEPT VARCHAR(50),
    SALARY INT
);

INSERT INTO EMP_INFO VALUES
(101, 'Ravi', 'HR', 6000),
(102, 'Anita', 'IT', 9000),
(103, 'Suman', 'Finance', 8500),
(104, 'John', 'IT', 7500),
(105, 'Priya', 'HR', 9500);

1️⃣ SUM() – Total of a Column
SELECT SUM(SALARY) AS Total_Salary FROM EMP_INFO;
🔸 Returns: 40500